function updateFunc(winner){
    srcy = document.getElementById(winner);
    
    console.log(srcy)

    // var element = document.getElementById('txt');
    // var text = element.innerText || element.textContent;
    // element.innerHTML = text;
}